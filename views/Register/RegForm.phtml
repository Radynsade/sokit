<section>
    <header>
        <h1>Регистрация</h1>
    </header>
    <article>
        <?php if (!empty($this->errorMessage)) { ?>
            <div class="error-message"><?= $this->errorMessage ?></div>
        <?php } ?>
        <form data-action="CreateUser">
            <label class="field">
                <div>Имя пользователя</div>
                <input type="text" name="username" minlength="4" maxlength="32" required>
            </label>
            <label class="field">
                <div>Придумайте пароль</div>
                <input type="password" name="newPassword" minlength="8" maxlength="64" required>
            </label>
            <label class="field">
                <div>Повторите пароль</div>
                <input type="password" name="repeatPassword" minlength="8" maxlength="64" required>
            </label>
            <div class="field-set">
                <input type="submit" name="completeReg" value="Завершить">
                <a href="/">Вернуться назад</a>
            </div>
        </form>
    </article>
</section>